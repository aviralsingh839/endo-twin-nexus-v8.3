# ENDO-TWIN V8.5 Build Guide

```bash
./START.sh
./setup_android.sh
./build_apks.sh all
```

Workstations: `./START.sh doctor` and `./START.sh patient-pc`.

Phone: Doctor → Mobile Link → address/code; Patient Android → Connect → Pair → Send latest session.
