# API Guide

Target boundary: Desktop / Android / future research clients → ENDO-TWIN service boundary → shared core services → module registry → disease modules.

FastAPI is a staged service-boundary option. The current application remains local-first/offline and does not require a network server to run its core research workflow.
